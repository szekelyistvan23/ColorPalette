package szekelyistvan.com.colorpalette.widget;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

import szekelyistvan.com.colorpalette.R;

public class PaletteWidgetConfiguration extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_palette_widget_configuration);
    }
}
